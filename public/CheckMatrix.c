#include "CheckMatrix.h"

/// native       0
/// pthread      1
/// nvcc         2

// 默认使用未加速
#define WORK_TYPE 0

#if (WORK_TYPE == 0)

#endif // WORK_TYPE == 0

#if (WORK_TYPE == 1)

#endif // WORK_TYPE == 1

#if (WORK_TYPE == 2)

#endif // WORK_TYPE == 2

